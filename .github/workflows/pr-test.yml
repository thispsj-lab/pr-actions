name: ci
on:
  pull_request:
    paths:
      - 'scripts/**'
jobs:
  slow-job:
    runs-on: windows-latest
    steps:
      - run: 'sleep 180'
  build:
    needs: [slow-job]
    runs-on: windows-latest
    steps:
      - run: ' echo "Build succeeded" '
