name: ci
on:
  pull_request:
    paths-ignore:
      - 'scripts/**'
jobs:
  setup-build:
    runs-on: ubuntu-latest
    if: ${{ on.pull_request.paths[*] != 'scripts/**' }}
    steps:
      - run: echo Yeah
  build:
    runs-on: windows-latest
    steps:
      - run: ' echo "Build not needed" '
